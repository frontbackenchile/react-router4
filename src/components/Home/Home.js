// Según Main.js, esta es la vista que se renderizará cuando
// la ruta concuerde con "/". Como vemos, en este caso es una
// vista súper básica: un h1.
import React from "react";

const Home = () => (
  <div>
    <h1>Welcome to the Tornadoes Website!</h1>
  </div>
)

export default Home;